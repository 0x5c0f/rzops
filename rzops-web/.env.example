# ============================================================
# RzOps Web 前端环境变量模板（vite dev / preview 使用）
# 复制为 rzops-web/.env 后按需修改；.env 不入库，本模板入库。
# ============================================================

# ---- 公网反代/内网穿透开发（可选） ----
# 本地开发（npm run dev / npm run preview）通过公网域名反代或内网穿透访问时设置，
# 例如 RZOPS_PUBLIC_HOST=cmdb.example.com。
# 作用：vite allowedHosts 白名单自动加入该域名（含子域通配），并自动禁用 HMR
# （公网反代未转发 WebSocket 时，HMR 连接失败会导致页面反复刷新）。
# 本地直连开发（localhost）无需设置；生产 web 为静态构建（nginx 托管），不受此变量影响。
RZOPS_PUBLIC_HOST=
